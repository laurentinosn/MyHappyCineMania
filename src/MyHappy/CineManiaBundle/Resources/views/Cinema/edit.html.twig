{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Editar Cinema</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('cinema') }}">
            Voltar para a Lista
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
