{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Criar Pessoa</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('pessoa') }}">
            Voltar para a Lista
        </a>
    </li>
</ul>
{% endblock %}
